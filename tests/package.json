{
  "name": "chess-move-validator-tests",
  "private": true,
  "type": "module",
  "description": "Validation harness: run after each change to check new behavior and backward compatibility.",
  "dependencies": { "playwright": "^1.48.0" }
}
